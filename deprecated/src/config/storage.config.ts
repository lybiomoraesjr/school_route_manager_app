export const STORAGE_PREFIX = "@school-route-manager";

export const COOKIES_KEYS = {
	AUTH_TOKEN: `${STORAGE_PREFIX}:authToken`,
};

export const LOCAL_STORAGE_KEYS = {
	USER: `${STORAGE_PREFIX}:user`,
};
