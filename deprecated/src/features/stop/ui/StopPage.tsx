import { StopsTable } from "@/features/stop/components/StopsTable";
import { Container } from "@mantine/core";

const StopPage = () => {
	return (
		<Container fluid>
			<StopsTable />
		</Container>
	);
};

export default StopPage;
